[package]
name = "datafusion-postgres"
description = "Exporting datafusion query engine with postgres wire protocol"
version = "0.18.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
keywords.workspace = true
homepage.workspace = true
repository.workspace = true
documentation.workspace = true
readme = "../README.md"
rust-version.workspace = true

[dependencies]
arrow-pg = { path = "../arrow-pg", version = "0.15.0", default-features = false, features = ["datafusion"] }
bytes.workspace = true
async-trait = "0.1"
chrono.workspace = true
datafusion.workspace = true
datafusion-pg-catalog = { path = "../datafusion-pg-catalog", version = "0.18.3" }
datafusion-pg-functions = { path = "../datafusion-pg-functions", version = "0.1.0" }
geodatafusion = { version = "0.5", optional = true }
futures.workspace = true
getset = "0.1"
log = "0.4"
pgwire = { workspace = true, features = ["server-api-ring"] }
postgres-types.workspace = true
rust_decimal.workspace = true
tokio = { version = "1.52", features = ["sync", "net"] }
tokio-rustls = { version = "0.26", default-features = false, features = ["ring"] }
rustls-pemfile = "2.0"
rustls-pki-types = "1.14"

[dev-dependencies]
env_logger = "0.11"
pgvector = { version = "0.4", features = ["postgres"] }
postgres = "0.19"

[features]
default = []
postgis = ["geodatafusion", "arrow-pg/postgis"]
# pgvector compatibility: `vector(n)` column type plus the `<->` / `<#>` /
# `<=>` distance operators, backed by DataFusion's built-in array distance
# functions. See datafusion-pg-catalog / arrow-pg `pgvector` features.
pgvector = ["datafusion-pg-catalog/pgvector", "arrow-pg/pgvector"]
