[package]
name = "datafusion-pg-catalog"
description = "pg_catalog compatibility for datafusion"
version = "0.18.3"
edition.workspace = true
license.workspace = true
authors.workspace = true
keywords.workspace = true
homepage.workspace = true
repository.workspace = true
documentation.workspace = true
readme = "../README.md"
rust-version.workspace = true
include = [
  "src/**/*",
  "pg_catalog_arrow_exports/**/*",
  "Cargo.toml"
]

[dependencies]
async-trait = "0.1"
datafusion = { workspace = true, features = ["sql"] }
arrow-pg = { path = "../arrow-pg", version = "0.15.0", default-features = false, features = ["datafusion"] }
futures.workspace = true
log = "0.4"
postgres-types.workspace = true
tokio = { version = "1.52", features = ["sync"] }

[features]
# pgvector compatibility: recognize the pgvector `vector(n)` SQL type and the
# `<->` / `<#>` / `<=>` distance operators. Reuses DataFusion's built-in
# array_distance / inner_product / cosine_distance functions.
pgvector = []

[dev-dependencies]
env_logger = "0.11"
criterion = { version = "0.8", features = ["html_reports"] }

[[bench]]
name = "parser_benchmark"
harness = false
